openapi: 3.1.0
info:
  title: eth_uninstallFilter - Base
  version: '1.0'
servers:
  - url: https://{network}.g.alchemy.com/v2
    variables:
      network:
        enum:
          - base-mainnet
          - base-goerli
        default: base-mainnet
paths:
  /{apiKey}:
    post:
      summary: eth_uninstallFilter - Base
      $ref: ../ethereum/eth_uninstallFilter.yaml#/components/pathItems/path/post
      operationId: eth-uninstallfilter-base
