openapi: 3.1.0
info:
  title: eth_call - Base
  version: '1.0'
servers:
  - url: https://{network}.g.alchemy.com/v2
    variables:
      network:
        enum:
          - base-mainnet
          - base-goerli
        default: base-mainnet
paths:
  /{apiKey}:
    post:
      $ref: ../components/evm.yaml#/EthCall
      summary: eth_call - Base
      operationId: eth-call-base
      x-readme:
        explorer-enabled: false
        samples-languages:
          - curl
          - javascript
          - python
