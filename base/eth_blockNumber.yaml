openapi: 3.1.0
info:
  title: eth_blockNumber - Base
  version: '1.0'
servers:
  - url: https://{network}.g.alchemy.com/v2
    variables:
      network:
        enum:
          - base-mainnet
          - base-goerli
        default: base-mainnet
paths:
  /{apiKey}:
    post:
      $ref: ../components/evm.yaml#/EthBlockNumber
      summary: eth_blockNumber - Base
      operationId: eth-blocknumber-base
      x-readme:
        samples-languages:
          - curl
          - javascript
          - python
