openapi: 3.1.0
info:
  title: eth_gasPrice - Base
  version: '1.0'
servers:
  - url: https://{network}.g.alchemy.com/v2
    variables:
      network:
        enum:
          - base-mainnet
          - base-goerli
        default: base-mainnet
paths:
  /{apiKey}:
    post:
      summary: eth_gasPrice - Base
      $ref: ../ethereum/eth_gasPrice.yaml#/components/pathItems/path/post
      operationId: eth-gasprice-base
      x-readme:
        samples-languages:
          - curl
          - javascript
          - python
