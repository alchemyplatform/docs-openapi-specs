openapi: 3.1.0
info:
  title: net_listening - optimism
  version: '1.0'
servers:
  - url: 'https://opt-mainnet.g.alchemy.com/v2/{apiKey}'
    variables:
      apiKey:
        default: demo
paths:
  /:
    post:
      $ref: ../ethereum/net_listening.yaml#/components/pathItems/path/post
      operationId: net-listening-optimism
