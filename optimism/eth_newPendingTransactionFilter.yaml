openapi: 3.1.0
info:
  title: eth_newPendingTransactionFilter - Optimism
  version: '1.0'
servers:
  - url: 'https://{network}.g.alchemy.com/v2/'
    variables:
      network:
        enum:
        - opt-mainnet
        - opt-goerli
        default: opt-mainnet
paths:
  /{apiKey}:
    post:
      summary: eth_newPendingTransactionFilter - Optimism
      $ref: ../ethereum/eth_newPendingTransactionFilter.yaml#/components/pathItems/path/post
      operationId: eth-newpendingtransactionfilter-optimism
