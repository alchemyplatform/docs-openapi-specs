openapi: 3.1.0
info:
  title: bor_getCurrentValidators - Polygon PoS
  version: '1.0'
servers:
  - url: 'https://{network}.g.alchemy.com/v2/'
    variables:
      network:
        enum:
        - polygon-mainnet
        - polygon-mumbai
        default: polygon-mainnet
paths:
  /{apiKey}:
    post:
      summary: bor_getCurrentValidators - Polygon PoS
      description: 'Returns current validators for Polygon.'
      tags: []
      parameters:
        - $ref: ../components/parameters.yaml#/ApiKey
      requestBody:
        content:
          application/json:
            schema:
              $ref: ../evm_body.yaml#/bor_getCurrentValidators
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: ../evm_responses.yaml#/bor_getCurrentValidators
      operationId: bor-getCurrentValidators-polygon
