openapi: 3.1.0
info:
  title: eth_getTransactionReceiptsByBlock - Polygon PoS
  version: '1.0'
servers:
  - url: 'https://{network}.g.alchemy.com/v2/'
    variables:
      network:
        enum:
          - polygon-mainnet
          - polygon-mumbai
        default: polygon-mainnet
x-sandbox:
  category:
    type:
      $ref: '../components/sandbox.yaml#/Category'
    value: core
paths:
  /{apiKey}:
    post:
      tags: []
      summary: eth_getTransactionReceiptsByBlock - Polygon PoS
      description: 'Polygon API - Returns all transaction receipts for the given block number or hash.'
      parameters:
        - $ref: ../components/parameters.yaml#/ApiKey
      requestBody:
        content:
          application/json:
            schema:
              $ref: ../evm_body.yaml#/eth_getTransactionReceiptsByBlock
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: ../evm_responses.yaml#/eth_getTransactionReceiptsByBlock
      operationId: eth-gettransactionreceiptsbyblock-polygon
