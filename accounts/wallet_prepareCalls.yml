# This file is generated by scripts/generate-schema.ts
openapi: 3.0.1
info:
  title: wallet_prepareCalls
  version: 1.0.0
servers:
  - url: https://api.g.alchemy.com
paths:
  /{apiKey}:
    post:
      summary: wallet_prepareCalls
      description: This method is used to prepare a user operation for submission. It will return a built user operation and a signature request which needs to be signed by the user before submitting to wallet_sendPreparedCalls. This method is only available as a private early access for now
      x-readme:
        samples-languages:
          - javascript
          - curl
          - python
          - go
      parameters:
        - name: apiKey
          in: path
          schema:
            type: string
            default: docs-demo
          description: |
            <style>
              .custom-style {
                color: #048FF4;
              }
            </style>
            For higher throughput, <span class="custom-style"><a href="https://alchemy.com/?a=docs-demo" target="_blank">create your own API key</a></span>
          required: true

      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: integer
                  default: 1
                jsonrpc:
                  type: string
                  default: "2.0"
                  enum:
                    - "2.0"
                method:
                  type: string
                  default: wallet_prepareCalls
                  enum:
                    - wallet_prepareCalls
                params:
                  $ref: ./accounts/wallet_prepareCalls.json#/request
      responses:
        "200":
          description: "Success"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
components:
  schemas:
    Response:
      $ref: ./accounts/wallet_prepareCalls.json#/response
