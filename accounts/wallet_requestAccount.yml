# This file is generated by scripts/generate-schema.ts
openapi: 3.0.1
info:
  title: wallet_requestAccount
  version: 1.0.0
servers:
  - url: https://api.g.alchemy.com
paths:
  /{apiKey}:
    post:
      summary: wallet_requestAccount
      description: This method is used to get the smart account address and relevant info for a given signer. If an account does not already exist for a given signer, this method will create one before returning the counterfactual address. This method requires passing a stamped whoami request via the x-alchemy-wallet-api-auth header.. This method is only available as a private early access for now
      x-readme:
        samples-languages:
          - javascript
          - curl
          - python
          - go
      parameters:
        - name: apiKey
          in: path
          schema:
            type: string
            default: docs-demo
          description: |
            <style>
              .custom-style {
                color: #048FF4;
              }
            </style>
            For higher throughput, <span class="custom-style"><a href="https://alchemy.com/?a=docs-demo" target="_blank">create your own API key</a></span>
          required: true
        - name: x-alchemy-wallet-api-auth
          in: header
          description: The auth header is used to verify the end user making the request. When passed as a header, this object needs to be converted to string and Base64 encoded. See the schema definition for x-alchemy-wallet-api-auth for its shape
          schema:
            type: string
          required: true

      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: integer
                  default: 1
                jsonrpc:
                  type: string
                  default: "2.0"
                  enum:
                    - "2.0"
                method:
                  type: string
                  default: wallet_requestAccount
                  enum:
                    - wallet_requestAccount
                params:
                  $ref: ./accounts/wallet_requestAccount.json#/request
      responses:
        "200":
          description: "Success"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
components:
  schemas:
    Response:
      $ref: ./accounts/wallet_requestAccount.json#/response
    x-alchemy-wallet-api-auth:
      $ref: ./accounts/wallet_requestAccount.json#/headers/x-alchemy-wallet-api-auth
