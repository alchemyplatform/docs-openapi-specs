# This file is generated by scripts/generate-schema.ts
openapi: 3.0.1
info:
  title: wallet_sendPreparedCalls
  version: 1.0.0
servers:
  - url: https://api.g.alchemy.com
paths:
  /{apiKey}:
    post:
      summary: wallet_sendPreparedCalls
      description: This method is used after signing the signatureRequest returned from prepareCalls to submit a user operation. This method is only available as a private early access for now
      x-readme:
        samples-languages:
          - javascript
          - curl
          - python
          - go
      parameters:
        - name: apiKey
          in: path
          schema:
            type: string
            default: docs-demo
          description: |
            <style>
              .custom-style {
                color: #048FF4;
              }
            </style>
            For higher throughput, <span class="custom-style"><a href="https://alchemy.com/?a=docs-demo" target="_blank">create your own API key</a></span>
          required: true

      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: integer
                  default: 1
                jsonrpc:
                  type: string
                  default: "2.0"
                  enum:
                    - "2.0"
                method:
                  type: string
                  default: wallet_sendPreparedCalls
                  enum:
                    - wallet_sendPreparedCalls
                params:
                  $ref: ./accounts/wallet_sendPreparedCalls.json#/request
      responses:
        "200":
          description: "Success"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
components:
  schemas:
    Response:
      $ref: ./accounts/wallet_sendPreparedCalls.json#/response
