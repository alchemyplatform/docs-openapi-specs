openapi: 3.1.0
info:
  title: Validate Block Hash
  version: '1.0'
servers:
  - url: 'https://solana-mainnet.g.alchemy.com/v2'
paths:
  /{apiKey}:
    post:
      summary: isBlockhashValid
      description: Returns whether a blockhash is still valid or not.
      tags: []
      parameters:
        - name: apiKey
          in: path
          schema:
            type: string
            default: demo
          required: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: solana_body.yaml#/isBlockhashValid
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: solana_responses.yaml#/isBlockhashValid
      operationId: isBlockhashValid
      x-readme:
        explorer-enabled: false
