openapi: 3.1.0
info:
  title: eth_accounts - Arbitrum
  version: '1.0'
servers:
  - url: 'https://{network}.g.alchemy.com/v2/'
    variables:
      network:
        enum:
          - arb-mainnet
          - arb-goerli
        default: arb-mainnet
paths:
  /{apiKey}:
    post:
      $ref: ../components/evm.yaml#/EthAccount
      summary: eth_accounts - Arbitrum
      operationId: eth-accounts-arbitrum
