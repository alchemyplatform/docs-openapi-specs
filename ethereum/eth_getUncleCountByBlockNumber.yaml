openapi: 3.1.0
info:
  title: Uncle Count By Number
  version: '1.0'
servers:
  - url: 'https://eth-mainnet.alchemyapi.io/v2'
    variables:
      apiKey:
        default: demo
paths:
  '/{apiKey}':
    post:
      summary: eth_getUncleCountByBlockNumber
      description: "Returns the number of uncles in a block matching the give block number."
      tags: []
      parameters:
        - name: apiKey
          in: path
          schema:
            type: string
            default: demo
          required: true
      requestBody:
        description: "Accepts integer of a block number, or the string 'latest', 'earliest' or 'pending'."
        content:
          application/json:
            schema:
              $ref: body.yaml#/eth_getUncleCountByBlockNumber
      responses:
        '200':
          description: "Returns integer of the number of uncles in this block."
          content:
            application/json:
              schema:
                $ref: 'responses.yaml#/string_result_response'
      operationId: eth_getUncleCountByBlockNumber
